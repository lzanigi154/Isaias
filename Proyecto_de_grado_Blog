<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Proyecto de Grado — Noemí Cosmetics</title>
  <style>
    /* Paleta del usuario */
    :root{--bg:#FFFFFF;--text:#272727;--muted:#6E6D6C;--muted-2:#9E9E9D;--surface:#F7F7F7;--accent:#272727;--card:#FFFFFF;--maxw:1100px}
    *{box-sizing:border-box}
    body{font-family:Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial; margin:0;background:var(--surface);color:var(--text);-webkit-font-smoothing:antialiased}
    a{color:var(--accent);text-decoration:none}
    header{background:linear-gradient(180deg, #ffffff 0%, #fbfbfb 100%);border-bottom:1px solid #eee}
    .wrap{max-width:var(--maxw);margin:0 auto;padding:0 20px}
    .topbar{display:flex;align-items:center;gap:16px;padding:18px 0}
    .site-brand{display:flex;align-items:center;gap:12px}
    .logo-mark{width:46px;height:46px;border-radius:10px;background:var(--text);display:inline-block}
    .site-title{font-weight:700;font-size:18px}
    .site-desc{font-size:13px;color:var(--muted)}
    nav.main-nav{margin-left:auto}
    nav.main-nav ul{list-style:none;margin:0;padding:0;display:flex;gap:12px}
    nav.main-nav a{padding:10px 12px;border-radius:8px;font-weight:600;color:var(--text)}
    nav.main-nav a:hover{background:#f0f0f0}

    /* Hero */
    .hero{display:grid;grid-template-columns:1fr;gap:16px;padding:36px 0}
    @media(min-width:900px){.hero{grid-template-columns:2fr 1fr;align-items:center}}
    .hero-card{background:var(--card);padding:28px;border-radius:12px;box-shadow:0 8px 20px rgba(39,39,39,0.06)}
    .hero h1{margin:0 0 8px;font-size:28px}
    .hero p.lead{margin:0;color:var(--muted);line-height:1.6}

    /* Layout */
    .layout{display:grid;grid-template-columns:2fr 1fr;gap:20px;margin-top:18px}
    @media(max-width:900px){.layout{grid-template-columns:1fr}}
    .card{background:var(--card);padding:18px;border-radius:10px;box-shadow:0 6px 18px rgba(39,39,39,0.04)}
    .post{display:flex;gap:14px}
    .post img{width:220px;height:140px;object-fit:cover;border-radius:8px}
    .post h3{margin:0}
    .meta{font-size:13px;color:var(--muted)}

    .sidebar .widget{margin-bottom:12px}
    .widget h4{margin:0 0 10px}
    .category-list li{list-style:none;margin-bottom:8px}

    /* Post single */
    .post-content img{max-width:100%;border-radius:8px}
    .tags{display:flex;gap:8px;flex-wrap:wrap}
    .tag{background:#f3f3f3;padding:6px 10px;border-radius:999px;font-size:13px;color:var(--muted)}

    /* Footer */
    footer{padding:28px 0;margin-top:28px;border-top:1px solid #eee}
    .footer-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
    @media(max-width:800px){.footer-grid{grid-template-columns:1fr}}

    /* Buttons */
    .btn{display:inline-block;padding:10px 14px;border-radius:10px;background:var(--text);color:#fff;font-weight:700}

    /* Small helpers */
    .small{font-size:13px;color:var(--muted)}
    .muted{color:var(--muted)}
    .center{text-align:center}

    /* Responsive utilities */
    .hidden-mobile{display:block}
    @media(max-width:700px){.hidden-mobile{display:none}}
  </style>
</head>
<body>
  <header>
    <div class="wrap topbar">
      <div class="site-brand">
        <div class="logo-mark" aria-hidden="true"></div>
        <div>
          <div class="site-title">Proyecto de Grado — Noemí Cosmetics</div>
          <div class="site-desc">Diseño de página web e-commerce para Noemí Cosmetics</div>
        </div>
      </div>

      <nav class="main-nav" aria-label="Menú principal">
        <ul>
          <li><a href="#/">Inicio</a></li>
          <li><a href="#/blog">Blog</a></li>
          <li><a href="#/apd">Proyectos APD</a></li>
          <li><a href="#/ppr">Proyectos PPR</a></li>
          <li><a href="#/podcast">Podcast</a></li>
          <li><a href="#/portafolio">Portafolio</a></li>
          <li><a href="#/acerca">Acerca</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main class="wrap">
    <div id="app">
      <!-- Hero + sidebar quick -->
      <section class="hero">
        <div class="hero-card">
          <h1>Diseño de sitio web e-commerce para Noemí Cosmetics</h1>
          <p class="lead">Este proyecto de grado propone la creación de una tienda en línea intuitiva y accesible para Noemí Cosmetics, enfocada en mejorar la experiencia de compra, la gestión de inventario y la conversión de ventas mediante buenas prácticas de UX y diseño visual.</p>
          <p style="margin-top:12px"><strong>Autor:</strong> Isaias Alanoca · <span class="small">Julian Isaias Alanoca Ticona</span></p>
          <p style="margin-top:16px"><a class="btn" href="#/blog">Ver entradas del proyecto</a></p>
        </div>

        <aside class="card hidden-mobile">
          <h4>Resumen</h4>
          <p class="small">Páginas incluidas: Inicio, Blog, Proyectos APD, Proyectos PPR, Podcast, Portafolio, Acerca de. Soporta: categorías, etiquetas, imagen destacada y comentarios (simulados).</p>
          <div style="margin-top:12px">
            <h4>Contacto</h4>
            <p class="small">Email: <a href="mailto:ticonalanoca@gmail.com">ticonalanoca@gmail.com</a><br>Tel: +591 67030877</p>
          </div>
        </aside>
      </section>

      <!-- Layout -->
      <div class="layout">
        <section id="main-column">
          <!-- Content rendered by JS -->
        </section>

        <aside class="sidebar">
          <div class="card widget">
            <h4>Buscar</h4>
            <input id="q" placeholder="Buscar entradas..." style="width:100%;padding:10px;border-radius:8px;border:1px solid #eaeaea">
          </div>

          <div class="card widget">
            <h4>Categorías</h4>
            <ul class="category-list" id="category-list"></ul>
          </div>

          <div class="card widget">
            <h4>Etiquetas</h4>
            <div id="tag-list"></div>
          </div>

          <div class="card widget">
            <h4>Enlaces</h4>
            <p class="small">Puedes compartir este archivo HTML como un sitio estático. Para publicar, usa GitHub Pages o Netlify.</p>
          </div>
        </aside>
      </div>
    </div>
  </main>

  <footer>
    <div class="wrap">
      <div class="footer-grid">
        <div>
          <h4>Proyecto de Grado</h4>
          <p class="small">Diseño de e-commerce para Noemí Cosmetics — Entrega académica.</p>
        </div>
        <div>
          <h4>Autor</h4>
          <p class="small">Isaias Alanoca · <a href="mailto:ticonalanoca@gmail.com">ticonalanoca@gmail.com</a></p>
        </div>
        <div>
          <h4>Contacto</h4>
          <p class="small">Tel: +591 67030877</p>
        </div>
      </div>
      <div style="margin-top:18px" class="center small">© <span id="year"></span> — Proyecto de Grado — Noemí Cosmetics</div>
    </div>
  </footer>

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();

    // Site content: posts, categories, tags, projects, portfolio, podcast
    const SITE = {title:'Proyecto de Grado — Noemí Cosmetics', author:'Isaias Alanoca', email:'ticonalanoca@gmail.com'};

    const posts = [
      {id:'p1',title:'Análisis del problema: gestión de inventarios',date:'2025-09-12',category:'Proyectos',tags:['diagnóstico','inventario'],excerpt:'Diagnóstico sobre los desafíos en la gestión de inventario de Noemí Cosmetics y su impacto en operaciones.',image:'https://images.unsplash.com/photo-1581092334204-6b09c5c4b4a6?q=80&w=1200&auto=format&fit=crop',content:`<p>El diagnóstico identificó problemas en el control de stock causados por fluctuaciones de la demanda, falta de seguimiento de variantes y procesos manuales. Esto ocasiona roturas de stock y exceso de ciertos SKUs.</p><h4>Objetivos</h4><ul><li>Implementar un diseño de catálogo claro en la tienda.</li><li>Mejorar la interfaz de administración para gestión de inventario.</li></ul>`},

      {id:'p2',title:'Diseño de la solución: arquitectura y mockups',date:'2025-10-05',category:'Proyectos',tags:['ux','prototipo'],excerpt:'Descripción de la arquitectura propuesta, flujo de usuario y wireframes para la tienda e-commerce.',image:'https://images.unsplash.com/photo-1498050108023-c5249f4df085?q=80&w=1200&auto=format&fit=crop',content:`<p>Se propuso una arquitectura basada en categorías jerarquizadas, fichas de producto con variantes y un proceso de checkout simplificado. Se crearon mockups de alta fidelidad y un prototipo interactivo para pruebas.</p>`},

      {id:'p3',title:'Pruebas con usuarios y validación',date:'2025-10-20',category:'Proyectos',tags:['pruebas','usabilidad'],excerpt:'Resultados de las pruebas de usabilidad con usuarios representativos y mejoras aplicadas.',image:'https://images.unsplash.com/photo-1522202176988-66273c2fd55f?q=80&w=1200&auto=format&fit=crop',content:`<p>Se realizaron pruebas con 10 participantes. Se midió tiempo de tarea, tasa de éxito y se identificaron fricciones en el flujo de pago. Se aplicaron correcciones y se redujo la tasa de abandono en el prototipo.</p>`},

      {id:'p4',title:'Episodio: Verificado o Viral — Noticias falsas',date:'2025-10-28',category:'Podcast',tags:['podcast','desinformacion'],excerpt:'Resumen del episodio donde se discuten noticias falsas en redes sociales.',image:'https://images.unsplash.com/photo-1511671782779-c97d3d27a1d4?q=80&w=1200&auto=format&fit=crop',content:`<p>En "Verificado o Viral" analizamos cómo se propagan las noticias falsas y proponemos estrategias para la alfabetización mediática en comunidades locales.</p>`}
    ];

    const projectsAPD = [
      {title:'Investigación de mercado para Noemí Cosmetics',desc:'Estudio de público objetivo, competencia y comportamiento de compra.',image:'https://images.unsplash.com/photo-1558494949-6f1aef7a2b2f?q=80&w=1200&auto=format&fit=crop'},
      {title:'Propuesta de catálogo y estructura',desc:'Definición de categorías, atributos y filtros para el e-commerce.',image:'https://images.unsplash.com/photo-1515378791036-0648a3ef77b2?q=80&w=1200&auto=format&fit=crop'}
    ];

    const projectsPPR = [
      {title:'Implementación del prototipo',desc:'Desarrollo de prototipo interactivo y pruebas de usabilidad.',image:'https://images.unsplash.com/photo-1529333166437-7750a6dd5a70?q=80&w=1200&auto=format&fit=crop'},
      {title:'Guía de estilo y componentes',desc:'Sistema visual con tipografías, paleta y componentes reutilizables.',image:'https://images.unsplash.com/photo-1492724441997-5dc865305da7?q=80&w=1200&auto=format&fit=crop'}
    ];

    const portfolio = [
      {title:'Branding — Marca personal',desc:'Identidad visual completa para un creativo.',image:'https://images.unsplash.com/photo-1545239351-1141bd82e8a6?q=80&w=1200&auto=format&fit=crop'},
      {title:'Cartelería — Evento cultural',desc:'Serie de carteles y piezas para difusión de evento.',image:'https://images.unsplash.com/photo-1520975916260-3f6ec2b2494a?q=80&w=1200&auto=format&fit=crop'},
      {title:'Motion graphics — Video corto',desc:'Video motion graphics de 1 minuto para Godifive.',image:'https://images.unsplash.com/photo-1496307042754-b4aa456c4a2d?q=80&w=1200&auto=format&fit=crop'}
    ];

    // Router & renderers
    function router(){
      const hash = location.hash || '#/';
      if(hash==='#/'||hash==='#') return renderHome();
      if(hash.startsWith('#/blog')) return renderBlog();
      if(hash.startsWith('#/post/')) return renderPost(hash.replace('#/post/',''));
      if(hash.startsWith('#/apd')) return renderProjects('APD');
      if(hash.startsWith('#/ppr')) return renderProjects('PPR');
      if(hash.startsWith('#/podcast')) return renderPodcast();
      if(hash.startsWith('#/portafolio')) return renderPortfolio();
      if(hash.startsWith('#/acerca')) return renderAbout();
      renderHome();
    }

    function renderHome(){
      const main = document.getElementById('main-column');
      const latest = posts.slice().sort((a,b)=>b.date.localeCompare(a.date)).slice(0,3);
      main.innerHTML = `
        <div class="card">
          <h2>Últimas actividades</h2>
          <div style="display:grid;gap:12px;margin-top:12px">
            ${latest.map(p=>`
              <article class="post">
                <img src="${p.image}" alt="${p.title}">
                <div>
                  <h3><a href="#/post/${p.id}">${p.title}</a></h3>
                  <div class="meta">${p.date} · ${p.category}</div>
                  <p class="small">${p.excerpt}</p>
                </div>
              </article>
            `).join('')}
          </div>
        </div>

        <div class="card" style="margin-top:14px">
          <h2>Objetivo del proyecto</h2>
          <p class="small">Diseñar una tienda en línea (e-commerce) para Noemí Cosmetics que mejore la experiencia de compra, facilite la gestión de inventarios y aumente la conversión mediante un diseño claro y buenas prácticas de UX.</p>
        </div>
      `;
      populateSidebar();
    }

    function renderBlog(){
      const main = document.getElementById('main-column');
      main.innerHTML = `<div class="card"><h2>Blog</h2><div id="post-list" style="margin-top:12px"></div></div>`;
      const list = document.getElementById('post-list');
      const sorted = posts.slice().sort((a,b)=>b.date.localeCompare(a.date));
      list.innerHTML = sorted.map(p=>`
        <article class="post card" style="align-items:flex-start">
          <img src="${p.image}" alt="${p.title}">
          <div>
            <h3><a href="#/post/${p.id}">${p.title}</a></h3>
            <div class="meta">${p.date} · ${p.category}</div>
            <p class="small">${p.excerpt}</p>
            <div style="margin-top:8px"><a href="#/post/${p.id}" class="btn">Leer</a></div>
          </div>
        </article>
      `).join('');
      populateSidebar();
    }

    function renderPost(id){
      const p = posts.find(x=>x.id===id);
      const main = document.getElementById('main-column');
      if(!p){main.innerHTML='<div class="card"><h2>Entrada no encontrada</h2></div>';return}
      main.innerHTML = `
        <article class="card post-content">
          <h2>${p.title}</h2>
          <div class="meta">${p.date} · ${p.category} · ${p.tags.map(t=>`<span class=\"tag\">${t}</span>`).join(' ')}</div>
          <img src="${p.image}" alt="${p.title}" style="margin-top:12px;border-radius:8px">
          <div style="margin-top:12px">${p.content}</div>

          <section style="margin-top:18px">
            <h4>Comentarios</h4>
            <div id="comments-${p.id}"></div>
            <div style="margin-top:8px;display:flex;gap:8px;flex-wrap:wrap">
              <input id="c-name" placeholder="Tu nombre" style="padding:8px;border-radius:8px;border:1px solid #eaeaea;width:30%">
              <input id="c-text" placeholder="Comentario" style="padding:8px;border-radius:8px;border:1px solid #eaeaea;width:60%">
              <button id="c-send" class="btn">Enviar</button>
            </div>
          </section>
        </article>
      `;
      // comments localStorage
      const key = 'comments_'+p.id;
      const box = document.getElementById('comments-'+p.id);
      function loadComments(){
        const arr = JSON.parse(localStorage.getItem(key)||'[]');
        box.innerHTML = arr.map(c=>`<div style=\"border-top:1px solid #eee;padding-top:8px;margin-top:8px\"><strong>${c.name}</strong><div class=\"small\">${c.date}</div><p>${c.text}</p></div>`).join('')||'<div class="small">No hay comentarios.</div>';
      }
      loadComments();
      document.getElementById('c-send').addEventListener('click', ()=>{
        const name = document.getElementById('c-name').value || 'Anónimo';
        const text = document.getElementById('c-text').value || '';
        if(!text) return alert('Escribe un comentario.');
        const arr = JSON.parse(localStorage.getItem(key)||'[]');
        arr.push({name,text,date:new Date().toLocaleString()});
        localStorage.setItem(key, JSON.stringify(arr));
        document.getElementById('c-text').value='';
        loadComments();
      });
      populateSidebar();
    }

    function renderProjects(type){
      const main = document.getElementById('main-column');
      const list = type==='APD' ? projectsAPD : projectsPPR;
      main.innerHTML = `<div class="card"><h2>Proyectos ${type}</h2><div style=\"display:grid;gap:12px;margin-top:12px\">${list.map(p=>`<div style=\"display:flex;gap:12px;align-items:center\"><img src=\"${p.image}\" style=\"width:120px;height:80px;object-fit:cover;border-radius:8px\"><div><h3>${p.title}</h3><p class=\"small\">${p.desc}</p></div></div>`).join('')}</div></div>`;
      populateSidebar();
    }

    function renderPodcast(){
      const main = document.getElementById('main-column');
      const eps = posts.filter(p=>p.category==='Podcast');
      main.innerHTML = `<div class=\"card\"><h2>Podcast — Verificado o Viral</h2><p class=\"small\">Pequeño proyecto de audio donde se discuten noticias falsas y alfabetización mediática.</p><div style=\"margin-top:12px\">${eps.map(e=>`<article class=\"card\"><h3>${e.title}</h3><div class=\"meta\">${e.date}</div><p class=\"small\">${e.excerpt}</p><audio controls style=\"width:100%\"><source src=\"\" type=\"audio/mpeg\">Tu navegador no soporta audio.</audio></article>`).join('')}</div></div>`;
      populateSidebar();
    }

    function renderPortfolio(){
      const main = document.getElementById('main-column');
      main.innerHTML = `<div class=\"card\"><h2>Portafolio</h2><div style=\"display:grid;grid-template-columns:repeat(1,1fr);gap:12px\">${portfolio.map(p=>`<div class=\"card\" style=\"display:flex;gap:12px;align-items:center\"><img src=\"${p.image}\" style=\"width:120px;height:80px;object-fit:cover;border-radius:8px\"><div><h3>${p.title}</h3><p class=\"small\">${p.desc}</p></div></div>`).join('')}</div></div>`;
      populateSidebar();
    }

    function renderAbout(){
      const main = document.getElementById('main-column');
      main.innerHTML = `
        <div class="card">
          <h2>Acerca de</h2>
          <p class="small">Soy Julian Isaias Alanoca Ticona (Isaias Alanoca), estudiante de diseño gráfico en mi último año. Este sitio documenta el desarrollo de mi proyecto de grado: el diseño y prototipo de una tienda e-commerce para Noemí Cosmetics. Aquí comparto investigaciones, prototipos, pruebas con usuarios y el portafolio de trabajos relacionados.</p>

          <h4 style="margin-top:12px">Contacto</h4>
          <p class="small">Email: <a href="mailto:ticonalanoca@gmail.com">ticonalanoca@gmail.com</a><br>Tel: +591 67030877</p>

          <h4 style="margin-top:12px">Propósito del sitio</h4>
          <p class="small">Este sitio simula la estructura y apariencia de un blog hecho en WordPress, adaptado para entregas académicas. Está pensado para ser publicado como sitio estático y servir como portafolio y registro de actividades.</p>
        </div>
      `;
      populateSidebar();
    }

    function populateSidebar(){
      // categories
      const cats = Array.from(new Set(posts.map(p=>p.category))).sort();
      document.getElementById('category-list').innerHTML = cats.map(c=>`<li><a href=\"#/blog\">${c}</a></li>`).join('');
      // tags
      const tags = Array.from(new Set(posts.flatMap(p=>p.tags))).sort();
      document.getElementById('tag-list').innerHTML = tags.map(t=>`<span class=\"tag\" style=\"margin-right:8px\">${t}</span>`).join('');
    }

    // Search
    document.getElementById('q').addEventListener('input', e=>{
      const q = e.target.value.toLowerCase();
      const filtered = posts.filter(p=> (p.title+p.excerpt+p.content+(p.tags||[]).join(' ')).toLowerCase().includes(q));
      const main = document.getElementById('main-column');
      main.innerHTML = `<div class=\"card\"><h2>Búsqueda: ${q}</h2>${filtered.map(p=>`<article class=\"post\" style=\"margin-top:12px\"><img src=\"${p.image}\" style=\"width:180px;height:110px\"><div><h3><a href=\"#/post/${p.id}\">${p.title}</a></h3><div class=\"meta\">${p.date}</div><p class=\"small\">${p.excerpt}</p></div></article>`).join('')}</div>`;
    });

    // Init router
    window.addEventListener('hashchange', router);
    window.addEventListener('load', ()=>{router(); populateSidebar();});
  </script>
</body>
</html>
